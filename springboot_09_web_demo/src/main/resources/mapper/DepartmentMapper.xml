<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.ideal.mapper.DepartmentMapper">
    <select id="queryAllDepartment" resultType="cn.ideal.pojo.Department">
        select * from department
    </select>

    <select id="queryDepartmentById" parameterType="java.lang.Integer" resultType="cn.ideal.pojo.Department">
        select * from department where id = #{id}
    </select>

</mapper>